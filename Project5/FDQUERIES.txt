/* Made by Hreiðar Ólafur Arnarsson and Maciej Sierzputowski, 2016-11-03 */

SELECT 'Persons: ID-->PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT ID
  FROM Persons
 GROUP BY ID
 HAVING COUNT(DISTINCT PN) > 1
) X;
SELECT 'Persons: ID-->DID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT ID
  FROM Persons
 GROUP BY ID
 HAVING COUNT(DISTINCT DID) > 1
) X;
SELECT 'Persons: ID-->DN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT ID
  FROM Persons
 GROUP BY ID
 HAVING COUNT(DISTINCT DN) > 1
) X;
SELECT 'Persons: ID-->SSN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT ID
  FROM Persons
 GROUP BY ID
 HAVING COUNT(DISTINCT SSN) > 1
) X;
SELECT 'Persons: ID-->Z' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT ID
  FROM Persons
 GROUP BY ID
 HAVING COUNT(DISTINCT Z) > 1
) X;
SELECT 'Persons: ID-->T' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT ID
  FROM Persons
 GROUP BY ID
 HAVING COUNT(DISTINCT T) > 1
) X;
SELECT 'Persons: PN-->ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PN
  FROM Persons
 GROUP BY PN
 HAVING COUNT(DISTINCT ID) > 1
) X;
SELECT 'Persons: PN-->DID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PN
  FROM Persons
 GROUP BY PN
 HAVING COUNT(DISTINCT DID) > 1
) X;
SELECT 'Persons: PN-->DN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PN
  FROM Persons
 GROUP BY PN
 HAVING COUNT(DISTINCT DN) > 1
) X;
SELECT 'Persons: PN-->SSN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PN
  FROM Persons
 GROUP BY PN
 HAVING COUNT(DISTINCT SSN) > 1
) X;
SELECT 'Persons: PN-->Z' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PN
  FROM Persons
 GROUP BY PN
 HAVING COUNT(DISTINCT Z) > 1
) X;
SELECT 'Persons: PN-->T' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PN
  FROM Persons
 GROUP BY PN
 HAVING COUNT(DISTINCT T) > 1
) X;
SELECT 'Persons: DID-->ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DID
  FROM Persons
 GROUP BY DID
 HAVING COUNT(DISTINCT ID) > 1
) X;
SELECT 'Persons: DID-->PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DID
  FROM Persons
 GROUP BY DID
 HAVING COUNT(DISTINCT PN) > 1
) X;
SELECT 'Persons: DID-->DN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DID
  FROM Persons
 GROUP BY DID
 HAVING COUNT(DISTINCT DN) > 1
) X;
SELECT 'Persons: DID-->SSN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DID
  FROM Persons
 GROUP BY DID
 HAVING COUNT(DISTINCT SSN) > 1
) X;
SELECT 'Persons: DID-->Z' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DID
  FROM Persons
 GROUP BY DID
 HAVING COUNT(DISTINCT Z) > 1
) X;
SELECT 'Persons: DID-->T' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DID
  FROM Persons
 GROUP BY DID
 HAVING COUNT(DISTINCT T) > 1
) X;
SELECT 'Persons: DN-->ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DN
  FROM Persons
 GROUP BY DN
 HAVING COUNT(DISTINCT ID) > 1
) X;
SELECT 'Persons: DN-->PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DN
  FROM Persons
 GROUP BY DN
 HAVING COUNT(DISTINCT PN) > 1
) X;
SELECT 'Persons: DN-->DID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DN
  FROM Persons
 GROUP BY DN
 HAVING COUNT(DISTINCT DID) > 1
) X;
SELECT 'Persons: DN-->SSN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DN
  FROM Persons
 GROUP BY DN
 HAVING COUNT(DISTINCT SSN) > 1
) X;
SELECT 'Persons: DN-->Z' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DN
  FROM Persons
 GROUP BY DN
 HAVING COUNT(DISTINCT Z) > 1
) X;
SELECT 'Persons: DN-->T' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DN
  FROM Persons
 GROUP BY DN
 HAVING COUNT(DISTINCT T) > 1
) X;
SELECT 'Persons: SSN-->ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SSN
  FROM Persons
 GROUP BY SSN
 HAVING COUNT(DISTINCT ID) > 1
) X;
SELECT 'Persons: SSN-->PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SSN
  FROM Persons
 GROUP BY SSN
 HAVING COUNT(DISTINCT PN) > 1
) X;
SELECT 'Persons: SSN-->DID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SSN
  FROM Persons
 GROUP BY SSN
 HAVING COUNT(DISTINCT DID) > 1
) X;
SELECT 'Persons: SSN-->DN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SSN
  FROM Persons
 GROUP BY SSN
 HAVING COUNT(DISTINCT DN) > 1
) X;
SELECT 'Persons: SSN-->Z' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SSN
  FROM Persons
 GROUP BY SSN
 HAVING COUNT(DISTINCT Z) > 1
) X;
SELECT 'Persons: SSN-->T' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SSN
  FROM Persons
 GROUP BY SSN
 HAVING COUNT(DISTINCT T) > 1
) X;
SELECT 'Persons: Z-->ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT Z
  FROM Persons
 GROUP BY Z
 HAVING COUNT(DISTINCT ID) > 1
) X;
SELECT 'Persons: Z-->PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT Z
  FROM Persons
 GROUP BY Z
 HAVING COUNT(DISTINCT PN) > 1
) X;
SELECT 'Persons: Z-->DID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT Z
  FROM Persons
 GROUP BY Z
 HAVING COUNT(DISTINCT DID) > 1
) X;
SELECT 'Persons: Z-->DN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT Z
  FROM Persons
 GROUP BY Z
 HAVING COUNT(DISTINCT DN) > 1
) X;
SELECT 'Persons: Z-->SSN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT Z
  FROM Persons
 GROUP BY Z
 HAVING COUNT(DISTINCT SSN) > 1
) X;
SELECT 'Persons: Z-->T' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT Z
  FROM Persons
 GROUP BY Z
 HAVING COUNT(DISTINCT T) > 1
) X;
SELECT 'Persons: T-->ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT T
  FROM Persons
 GROUP BY T
 HAVING COUNT(DISTINCT ID) > 1
) X;
SELECT 'Persons: T-->PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT T
  FROM Persons
 GROUP BY T
 HAVING COUNT(DISTINCT PN) > 1
) X;
SELECT 'Persons: T-->DID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT T
  FROM Persons
 GROUP BY T
 HAVING COUNT(DISTINCT DID) > 1
) X;
SELECT 'Persons: T-->DN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT T
  FROM Persons
 GROUP BY T
 HAVING COUNT(DISTINCT DN) > 1
) X;
SELECT 'Persons: T-->SSN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT T
  FROM Persons
 GROUP BY T
 HAVING COUNT(DISTINCT SSN) > 1
) X;
SELECT 'Persons: T-->Z' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT T
  FROM Persons
 GROUP BY T
 HAVING COUNT(DISTINCT Z) > 1
) X;
SELECT 'Boats: BL-->BNo' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BL
  FROM Boats
 GROUP BY BL
 HAVING COUNT(DISTINCT BNo) > 1
) X;
SELECT 'Boats: BL-->Z' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BL
  FROM Boats
 GROUP BY BL
 HAVING COUNT(DISTINCT Z) > 1
) X;
SELECT 'Boats: BL-->T' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BL
  FROM Boats
 GROUP BY BL
 HAVING COUNT(DISTINCT T) > 1
) X;
SELECT 'Boats: BL-->BN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BL
  FROM Boats
 GROUP BY BL
 HAVING COUNT(DISTINCT BN) > 1
) X;
SELECT 'Boats: BL-->SSN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BL
  FROM Boats
 GROUP BY BL
 HAVING COUNT(DISTINCT SSN) > 1
) X;
SELECT 'Boats: BNo-->BL' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BNo
  FROM Boats
 GROUP BY BNo
 HAVING COUNT(DISTINCT BL) > 1
) X;
SELECT 'Boats: BNo-->Z' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BNo
  FROM Boats
 GROUP BY BNo
 HAVING COUNT(DISTINCT Z) > 1
) X;
SELECT 'Boats: BNo-->T' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BNo
  FROM Boats
 GROUP BY BNo
 HAVING COUNT(DISTINCT T) > 1
) X;
SELECT 'Boats: BNo-->BN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BNo
  FROM Boats
 GROUP BY BNo
 HAVING COUNT(DISTINCT BN) > 1
) X;
SELECT 'Boats: BNo-->SSN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BNo
  FROM Boats
 GROUP BY BNo
 HAVING COUNT(DISTINCT SSN) > 1
) X;
SELECT 'Boats: Z-->BL' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT Z
  FROM Boats
 GROUP BY Z
 HAVING COUNT(DISTINCT BL) > 1
) X;
SELECT 'Boats: Z-->BNo' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT Z
  FROM Boats
 GROUP BY Z
 HAVING COUNT(DISTINCT BNo) > 1
) X;
SELECT 'Boats: Z-->T' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT Z
  FROM Boats
 GROUP BY Z
 HAVING COUNT(DISTINCT T) > 1
) X;
SELECT 'Boats: Z-->BN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT Z
  FROM Boats
 GROUP BY Z
 HAVING COUNT(DISTINCT BN) > 1
) X;
SELECT 'Boats: Z-->SSN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT Z
  FROM Boats
 GROUP BY Z
 HAVING COUNT(DISTINCT SSN) > 1
) X;
SELECT 'Boats: T-->BL' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT T
  FROM Boats
 GROUP BY T
 HAVING COUNT(DISTINCT BL) > 1
) X;
SELECT 'Boats: T-->BNo' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT T
  FROM Boats
 GROUP BY T
 HAVING COUNT(DISTINCT BNo) > 1
) X;
SELECT 'Boats: T-->Z' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT T
  FROM Boats
 GROUP BY T
 HAVING COUNT(DISTINCT Z) > 1
) X;
SELECT 'Boats: T-->BN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT T
  FROM Boats
 GROUP BY T
 HAVING COUNT(DISTINCT BN) > 1
) X;
SELECT 'Boats: T-->SSN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT T
  FROM Boats
 GROUP BY T
 HAVING COUNT(DISTINCT SSN) > 1
) X;
SELECT 'Boats: BN-->BL' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BN
  FROM Boats
 GROUP BY BN
 HAVING COUNT(DISTINCT BL) > 1
) X;
SELECT 'Boats: BN-->BNo' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BN
  FROM Boats
 GROUP BY BN
 HAVING COUNT(DISTINCT BNo) > 1
) X;
SELECT 'Boats: BN-->Z' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BN
  FROM Boats
 GROUP BY BN
 HAVING COUNT(DISTINCT Z) > 1
) X;
SELECT 'Boats: BN-->T' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BN
  FROM Boats
 GROUP BY BN
 HAVING COUNT(DISTINCT T) > 1
) X;
SELECT 'Boats: BN-->SSN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BN
  FROM Boats
 GROUP BY BN
 HAVING COUNT(DISTINCT SSN) > 1
) X;
SELECT 'Boats: SSN-->BL' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SSN
  FROM Boats
 GROUP BY SSN
 HAVING COUNT(DISTINCT BL) > 1
) X;
SELECT 'Boats: SSN-->BNo' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SSN
  FROM Boats
 GROUP BY SSN
 HAVING COUNT(DISTINCT BNo) > 1
) X;
SELECT 'Boats: SSN-->Z' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SSN
  FROM Boats
 GROUP BY SSN
 HAVING COUNT(DISTINCT Z) > 1
) X;
SELECT 'Boats: SSN-->T' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SSN
  FROM Boats
 GROUP BY SSN
 HAVING COUNT(DISTINCT T) > 1
) X;
SELECT 'Boats: SSN-->BN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SSN
  FROM Boats
 GROUP BY SSN
 HAVING COUNT(DISTINCT BN) > 1
) X;
SELECT 'Projects: ID-->PID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT ID
  FROM Projects
 GROUP BY ID
 HAVING COUNT(DISTINCT PID) > 1
) X;
SELECT 'Projects: ID-->DID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT ID
  FROM Projects
 GROUP BY ID
 HAVING COUNT(DISTINCT DID) > 1
) X;
SELECT 'Projects: ID-->DN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT ID
  FROM Projects
 GROUP BY ID
 HAVING COUNT(DISTINCT DN) > 1
) X;
SELECT 'Projects: ID-->PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT ID
  FROM Projects
 GROUP BY ID
 HAVING COUNT(DISTINCT PN) > 1
) X;
SELECT 'Projects: ID-->DM' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT ID
  FROM Projects
 GROUP BY ID
 HAVING COUNT(DISTINCT DM) > 1
) X;
SELECT 'Projects: PID-->ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PID
  FROM Projects
 GROUP BY PID
 HAVING COUNT(DISTINCT ID) > 1
) X;
SELECT 'Projects: PID-->DID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PID
  FROM Projects
 GROUP BY PID
 HAVING COUNT(DISTINCT DID) > 1
) X;
SELECT 'Projects: PID-->DN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PID
  FROM Projects
 GROUP BY PID
 HAVING COUNT(DISTINCT DN) > 1
) X;
SELECT 'Projects: PID-->PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PID
  FROM Projects
 GROUP BY PID
 HAVING COUNT(DISTINCT PN) > 1
) X;
SELECT 'Projects: PID-->DM' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PID
  FROM Projects
 GROUP BY PID
 HAVING COUNT(DISTINCT DM) > 1
) X;
SELECT 'Projects: DID-->ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DID
  FROM Projects
 GROUP BY DID
 HAVING COUNT(DISTINCT ID) > 1
) X;
SELECT 'Projects: DID-->PID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DID
  FROM Projects
 GROUP BY DID
 HAVING COUNT(DISTINCT PID) > 1
) X;
SELECT 'Projects: DID-->DN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DID
  FROM Projects
 GROUP BY DID
 HAVING COUNT(DISTINCT DN) > 1
) X;
SELECT 'Projects: DID-->PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DID
  FROM Projects
 GROUP BY DID
 HAVING COUNT(DISTINCT PN) > 1
) X;
SELECT 'Projects: DID-->DM' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DID
  FROM Projects
 GROUP BY DID
 HAVING COUNT(DISTINCT DM) > 1
) X;
SELECT 'Projects: DN-->ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DN
  FROM Projects
 GROUP BY DN
 HAVING COUNT(DISTINCT ID) > 1
) X;
SELECT 'Projects: DN-->PID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DN
  FROM Projects
 GROUP BY DN
 HAVING COUNT(DISTINCT PID) > 1
) X;
SELECT 'Projects: DN-->DID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DN
  FROM Projects
 GROUP BY DN
 HAVING COUNT(DISTINCT DID) > 1
) X;
SELECT 'Projects: DN-->PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DN
  FROM Projects
 GROUP BY DN
 HAVING COUNT(DISTINCT PN) > 1
) X;
SELECT 'Projects: DN-->DM' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DN
  FROM Projects
 GROUP BY DN
 HAVING COUNT(DISTINCT DM) > 1
) X;
SELECT 'Projects: PN-->ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PN
  FROM Projects
 GROUP BY PN
 HAVING COUNT(DISTINCT ID) > 1
) X;
SELECT 'Projects: PN-->PID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PN
  FROM Projects
 GROUP BY PN
 HAVING COUNT(DISTINCT PID) > 1
) X;
SELECT 'Projects: PN-->DID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PN
  FROM Projects
 GROUP BY PN
 HAVING COUNT(DISTINCT DID) > 1
) X;
SELECT 'Projects: PN-->DN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PN
  FROM Projects
 GROUP BY PN
 HAVING COUNT(DISTINCT DN) > 1
) X;
SELECT 'Projects: PN-->DM' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT PN
  FROM Projects
 GROUP BY PN
 HAVING COUNT(DISTINCT DM) > 1
) X;
SELECT 'Projects: DM-->ID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DM
  FROM Projects
 GROUP BY DM
 HAVING COUNT(DISTINCT ID) > 1
) X;
SELECT 'Projects: DM-->PID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DM
  FROM Projects
 GROUP BY DM
 HAVING COUNT(DISTINCT PID) > 1
) X;
SELECT 'Projects: DM-->DID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DM
  FROM Projects
 GROUP BY DM
 HAVING COUNT(DISTINCT DID) > 1
) X;
SELECT 'Projects: DM-->DN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DM
  FROM Projects
 GROUP BY DM
 HAVING COUNT(DISTINCT DN) > 1
) X;
SELECT 'Projects: DM-->PN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT DM
  FROM Projects
 GROUP BY DM
 HAVING COUNT(DISTINCT PN) > 1
) X;
SELECT 'Courses: CID-->TID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT CID
  FROM Courses
 GROUP BY CID
 HAVING COUNT(DISTINCT TID) > 1
) X;
SELECT 'Courses: CID-->BID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT CID
  FROM Courses
 GROUP BY CID
 HAVING COUNT(DISTINCT BID) > 1
) X;
SELECT 'Courses: CID-->SID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT CID
  FROM Courses
 GROUP BY CID
 HAVING COUNT(DISTINCT SID) > 1
) X;
SELECT 'Courses: CID-->TN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT CID
  FROM Courses
 GROUP BY CID
 HAVING COUNT(DISTINCT TN) > 1
) X;
SELECT 'Courses: CID-->SN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT CID
  FROM Courses
 GROUP BY CID
 HAVING COUNT(DISTINCT SN) > 1
) X;
SELECT 'Courses: CID-->SY' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT CID
  FROM Courses
 GROUP BY CID
 HAVING COUNT(DISTINCT SY) > 1
) X;
SELECT 'Courses: TID-->CID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT TID
  FROM Courses
 GROUP BY TID
 HAVING COUNT(DISTINCT CID) > 1
) X;
SELECT 'Courses: TID-->BID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT TID
  FROM Courses
 GROUP BY TID
 HAVING COUNT(DISTINCT BID) > 1
) X;
SELECT 'Courses: TID-->SID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT TID
  FROM Courses
 GROUP BY TID
 HAVING COUNT(DISTINCT SID) > 1
) X;
SELECT 'Courses: TID-->TN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT TID
  FROM Courses
 GROUP BY TID
 HAVING COUNT(DISTINCT TN) > 1
) X;
SELECT 'Courses: TID-->SN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT TID
  FROM Courses
 GROUP BY TID
 HAVING COUNT(DISTINCT SN) > 1
) X;
SELECT 'Courses: TID-->SY' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT TID
  FROM Courses
 GROUP BY TID
 HAVING COUNT(DISTINCT SY) > 1
) X;
SELECT 'Courses: BID-->CID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BID
  FROM Courses
 GROUP BY BID
 HAVING COUNT(DISTINCT CID) > 1
) X;
SELECT 'Courses: BID-->TID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BID
  FROM Courses
 GROUP BY BID
 HAVING COUNT(DISTINCT TID) > 1
) X;
SELECT 'Courses: BID-->SID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BID
  FROM Courses
 GROUP BY BID
 HAVING COUNT(DISTINCT SID) > 1
) X;
SELECT 'Courses: BID-->TN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BID
  FROM Courses
 GROUP BY BID
 HAVING COUNT(DISTINCT TN) > 1
) X;
SELECT 'Courses: BID-->SN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BID
  FROM Courses
 GROUP BY BID
 HAVING COUNT(DISTINCT SN) > 1
) X;
SELECT 'Courses: BID-->SY' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT BID
  FROM Courses
 GROUP BY BID
 HAVING COUNT(DISTINCT SY) > 1
) X;
SELECT 'Courses: SID-->CID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SID
  FROM Courses
 GROUP BY SID
 HAVING COUNT(DISTINCT CID) > 1
) X;
SELECT 'Courses: SID-->TID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SID
  FROM Courses
 GROUP BY SID
 HAVING COUNT(DISTINCT TID) > 1
) X;
SELECT 'Courses: SID-->BID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SID
  FROM Courses
 GROUP BY SID
 HAVING COUNT(DISTINCT BID) > 1
) X;
SELECT 'Courses: SID-->TN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SID
  FROM Courses
 GROUP BY SID
 HAVING COUNT(DISTINCT TN) > 1
) X;
SELECT 'Courses: SID-->SN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SID
  FROM Courses
 GROUP BY SID
 HAVING COUNT(DISTINCT SN) > 1
) X;
SELECT 'Courses: SID-->SY' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SID
  FROM Courses
 GROUP BY SID
 HAVING COUNT(DISTINCT SY) > 1
) X;
SELECT 'Courses: TN-->CID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT TN
  FROM Courses
 GROUP BY TN
 HAVING COUNT(DISTINCT CID) > 1
) X;
SELECT 'Courses: TN-->TID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT TN
  FROM Courses
 GROUP BY TN
 HAVING COUNT(DISTINCT TID) > 1
) X;
SELECT 'Courses: TN-->BID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT TN
  FROM Courses
 GROUP BY TN
 HAVING COUNT(DISTINCT BID) > 1
) X;
SELECT 'Courses: TN-->SID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT TN
  FROM Courses
 GROUP BY TN
 HAVING COUNT(DISTINCT SID) > 1
) X;
SELECT 'Courses: TN-->SN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT TN
  FROM Courses
 GROUP BY TN
 HAVING COUNT(DISTINCT SN) > 1
) X;
SELECT 'Courses: TN-->SY' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT TN
  FROM Courses
 GROUP BY TN
 HAVING COUNT(DISTINCT SY) > 1
) X;
SELECT 'Courses: SN-->CID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SN
  FROM Courses
 GROUP BY SN
 HAVING COUNT(DISTINCT CID) > 1
) X;
SELECT 'Courses: SN-->TID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SN
  FROM Courses
 GROUP BY SN
 HAVING COUNT(DISTINCT TID) > 1
) X;
SELECT 'Courses: SN-->BID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SN
  FROM Courses
 GROUP BY SN
 HAVING COUNT(DISTINCT BID) > 1
) X;
SELECT 'Courses: SN-->SID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SN
  FROM Courses
 GROUP BY SN
 HAVING COUNT(DISTINCT SID) > 1
) X;
SELECT 'Courses: SN-->TN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SN
  FROM Courses
 GROUP BY SN
 HAVING COUNT(DISTINCT TN) > 1
) X;
SELECT 'Courses: SN-->SY' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SN
  FROM Courses
 GROUP BY SN
 HAVING COUNT(DISTINCT SY) > 1
) X;
SELECT 'Courses: SY-->CID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SY
  FROM Courses
 GROUP BY SY
 HAVING COUNT(DISTINCT CID) > 1
) X;
SELECT 'Courses: SY-->TID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SY
  FROM Courses
 GROUP BY SY
 HAVING COUNT(DISTINCT TID) > 1
) X;
SELECT 'Courses: SY-->BID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SY
  FROM Courses
 GROUP BY SY
 HAVING COUNT(DISTINCT BID) > 1
) X;
SELECT 'Courses: SY-->SID' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SY
  FROM Courses
 GROUP BY SY
 HAVING COUNT(DISTINCT SID) > 1
) X;
SELECT 'Courses: SY-->TN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SY
  FROM Courses
 GROUP BY SY
 HAVING COUNT(DISTINCT TN) > 1
) X;
SELECT 'Courses: SY-->SN' AS FD, CASE WHEN COUNT(*)=0 THEN 'Gildir' ELSE 'Gildir ekki' END AS VALIDITY
FROM(
 SELECT SY
  FROM Courses
 GROUP BY SY
 HAVING COUNT(DISTINCT SN) > 1
) X;
